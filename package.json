{
  "name": "edupresence",
  "version": "1.0.0",
  "description": "EduPresence - 3D Interactive Educational Attendance System",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev:client": "cd client && npm start",
    "dev:server": "cd server && npm run dev",
    "build:client": "cd client && npm run build",
    "build:server": "cd server && npm run build"
  }
}